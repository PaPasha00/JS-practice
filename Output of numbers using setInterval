let printNumbersTwo = function(from, to) {
  let current = from;
  
  let interval = setInterval(function() {
    console.log(current);
    if (current == to) {
      clearInterval(interval);
    }
    current++;
  }, 1000)
};

printNumbersTwo(5, 10);
