function solution(number){
  let arr = [];
  for (let i = 1; i < number; i++) {
    if (i % 3 == 0 || i % 5 == 0) {
      arr.push(i);
    }    
  }
  var l = 0;
  for (m of arr) {
    l += parseInt(m);
  }
  return l
}
